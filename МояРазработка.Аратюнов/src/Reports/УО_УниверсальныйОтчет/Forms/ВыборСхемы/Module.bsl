
&НаКлиенте
Процедура СписокСхемВыборЗначения(Элемент, Значение, СтандартнаяОбработка)

	ОповеститьОВыборе(Элемент.ТекущиеДанные.Значение);

КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Отчеты.УО_УниверсальныйОтчет.ПолучитьСписокМакетов(СписокСхем);

КонецПроцедуры
